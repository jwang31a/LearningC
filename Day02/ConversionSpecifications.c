#include <stdio.h>

int main(void) {
    int i = 10;
    int j = 128;
    float x = 1.128f;
    float y = 3838.1840;
    // printf("%d %d\n", i); //some compilers may not catch this but it looks like gcc does
    // printf("%d\n", i, j); //gcc will also catch this, but other compilers may notfasdfasd
}
